@model IEnumerable<QuickQuestions.Models.SurveyResult>

@{
    ViewData["Title"] = "Результаты опросов";
}

<div class="row justify-content-center">
    <div class="col-md-9">
        <h1>@ViewData["Title"] для пользователя @ViewData["UserName"]</h1>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Опрос
                    </th>
                    <th>
                        Дата
                    </th>
                    <th class="col-2"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Survey.Name)
                        </td>
                        <td>
                            @(item.DateCreated.ToLocalTime().ToString("g"))
                        </td>
                        <td>
                            <a asp-area="" asp-controller="SurveyResults" asp-action="Delete" asp-route-id="@item.ID">Удалить</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

